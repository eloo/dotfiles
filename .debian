#
# Debian specific config 
#

alias f2b="sudo fail2ban-client status | grep \"Jail list:\" | sed \"s/ //g\" | awk '{split(\$2,a,\",\");for(i in a) system(\"sudo fail2ban-client status \" a[i])}' | grep \"Status\|IP list\""
